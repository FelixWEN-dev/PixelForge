# 2D 游戏素材生成器依赖

# FastAPI 框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# 数据验证
pydantic>=2.0.0

# 阿里云 DashScope SDK (用于 qwen-image-edit-plus API)
dashscope>=1.13.0

# 环境变量管理
python-dotenv>=1.0.0

# HTTP 请求
requests>=2.31.0

# LangChain 工具 (可选，保留兼容)
langchain>=0.1.0

# 数据库
sqlalchemy>=2.0.0
alembic>=1.13.0
pymysql>=1.1.0
